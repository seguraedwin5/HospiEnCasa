namespace HospiEnCasa.app.Dominio
{
    public enum Genero
    {
        /*los enum son un tipo de valor definidos por un conjunto de constantes con nombre de tipo entero: es decir
            const int masculino = 1;
            const int femenino = 2;
            normalmente se utiliza para listas que tienen valores constantes propios de la clase
        */
        masculino,
        femenino
    }
}